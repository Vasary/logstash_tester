# LogStash GELF tester

The small utility can spawn some logs with all log levels.

### Usage
```bash
# docker build -t logstash-tester .
# docker run -it logstash-tester bin/console IP PORT MESSAGE
```
